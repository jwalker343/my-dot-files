apt-get install \
parallel \
git-crypt \
git-flow \
yarn \
jq \
golang-go \
nmap \
ipcalc \
python \
python-pip \
apt-transport-https \
lsb-release \
software-properties-common \
tldr \
httpie \
ruby
-y


# Figure out?
node
diff-so-fancy



sudo -H pip install thefuck

# Speeedtest-cli
pip install speedtest-cli
sudo ln -s ~/.local/lib/python2.7/site-packages/speedtest.py /usr/local/bin/speedtest-cli
chmod +x /usr/local/bin/speedtest-cli

# Helmfile
go get github.com/roboll/helmfile
sudo ln -s ~/go/bin/helmfile /usr/local/bin/helmfile

# KUBECTL
curl -s https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
echo "deb https://apt.kubernetes.io/ kubernetes-xenial main" | sudo tee -a /etc/apt/sources.list.d/kubernetes.list
sudo apt-get update
sudo apt-get install -y kubectl google-cloud-sdk

# HELM
curl https://raw.githubusercontent.com/helm/helm/master/scripts/get > get_helm.sh
chmod 700 get_helm.sh
./get_helm.sh
mkdir -p ~/.helm/plugins


#KUBECTX
sudo git clone https://github.com/ahmetb/kubectx /opt/kubectx
sudo ln -s /opt/kubectx/kubectx /usr/local/bin/kubectx
sudo ln -s /opt/kubectx/kubens /usr/local/bin/kubens

# z
sudo git clone https://github.com/rupa/z.git /opt/z
echo ". /opt/z/z.sh" >> ~/.profile

# fzf
sudo git clone --depth 1 https://github.com/junegunn/fzf.git /opt/fzf
/opt/fzf/install

#Azure cli
AZ_REPO=$(lsb_release -cs)
echo "deb [arch=amd64] https://packages.microsoft.com/repos/azure-cli/ $AZ_REPO main" | \
    sudo tee /etc/apt/sources.list.d/azure-cli.list
sudo apt-key --keyring /etc/apt/trusted.gpg.d/Microsoft.gpg adv \
     --keyserver packages.microsoft.com \
     --recv-keys BC528686B50D79E339D3721CEB3E94ADBE1229CF
sudo apt-get update
sudo apt-get install azure-cli